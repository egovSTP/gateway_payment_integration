# No provider configuration needed.
# Base URLs are hardcoded in the local entry XML files (DobraEndpoint.xml, SaoWalletEndpoint.xml).
# API keys and credentials are passed per-request via the normalized headers: apiKey, clientKey, idempotencyKey.
